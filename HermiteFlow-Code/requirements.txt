# HermiteFlow requirements.
#
# torch / torchvision are deliberately unpinned: install the build that
# matches your CUDA runtime first. Kaggle and Colab already ship one, so
# on those hosts install everything below EXCEPT torch/torchvision.
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# cupy is OPTIONAL and deliberately not listed. It is used only by the
# faster CUDA splatting kernel in Phase 4 (arch.splat_impl: cupy / auto).
# The default arch.splat_impl: torch needs nothing extra and profiles at
# ~2 ms per splat, which is a rounding error next to the flow estimator.
# If you do install it, run others/verify_hermiteflow.py first - it
# compares the two backends whenever cupy is importable.
#
#   pip install cupy-cuda12x   # optional

torch
torchvision

numpy
opencv-python
Pillow
scipy
scikit-image

omegaconf
easydict
PyYAML
tqdm
tensorboard

# --- required by the FlowFormer backbone only ---
yacs
einops
timm
loguru

# --- required by src/VSF.py and src/VTF.py only (GIMM-era flow scripts,
#     not part of the HermiteFlow pipeline) ---
# rawpy
